"""I want a test that can cycle over each page of my Django website and confirm
the existance of each web page"""

# psuedo
expected_page_urls = [
                    '/home/',
                    '/BensBlog/',
                    '/aboutme/',
                    '/resume/',
                    '/contactme/'
                    ]

# psuedo
"""for pageurl in expected_page_urls:
    var result = requestPage(pageurl)
    assertPageSuccess(result)
    """
